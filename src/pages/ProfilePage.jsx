import React, { Fragment } from 'react';

import { ProfileComp } from '../components';

const ProfilePage = () => {

    return (
        <Fragment>
            <ProfileComp />
        </Fragment>
    );
};

export default ProfilePage;
